# class-manager 設計藍圖與路線圖

> 教師支援系統**第二站**：班級管理系統（老師在教室投影用的課堂工具）。
> 上位規則：`Project/AGENTS.md`、`班級事務/CLAUDE.md`、本站 `class-manager/CLAUDE.md`。
> 三站架構：班網 class-website（家長學生・唯讀公開）／**班級管理系統 class-manager（老師・教室投影・本站）**／自學星圖 study-orbit（學生・自學，尚未建）。三站獨立 repo，共用 Notion 後台。

## 設計原則（動工前必守）
- **純靜態、零相依、斷網照常**：無 build、無 npm、無外部 CDN/字型。教室會斷網，斷網時全部工具照常可用。
- **連網只做漸進增強**：讀「同域名姊妹站」班網的公開 JSON（同源、免 token、不改班網檔）；先畫本機快取→再抓雲端→離線用快取。
- **只存座號、不存姓名**：座號在工作台設定（localStorage）；姓名/查詢碼/個資一個字都不寫。
- **寫入分級**：L1 即時只寫 localStorage（Phase 1 全部工具都在此級，不碰網路寫入）；L2 事件即時送出（尚未做）。
- **視覺基準**：參照「智慧教室儀表板 2.0」（apaulliao.github.io/schooltool）——近黑星空底、玻璃圓角卡片、彩色膠囊工具列。色票正本 `assets/css/{projection,style}.css` 的 `:root`。
- **投影版面**：1600×900、主要內容 ≥40px、深色高對比；**全站 RWD**（@media 900/560，390 寬須無橫捲）。

## Phase 1（已完工・2026-09-04）
六支課堂工具全部上線，桌機投影＋手機皆可用。

| 工具 | 檔案 | 重點 | 連網 |
|---|---|---|---|
| 電子看板 | blackboard.html | 直式聯絡簿（數字直書）・值日生輪值・**午餐值週生**・今日課表・月相時鐘・日期含星期與週次・**節慶倒數**・語音廣播（**配字幕**）・計時・上下課鐘聲（**配字幕**） | 讀 contactbook／schedule／countdown／class-rules／lunch／weeks.json |
| 抽籤 | draw.html | 不重複、每人限中一次、每日重置 | 無 |
| 計時 | timer.html | 大字倒數＋碼錶、預設/自訂、暫停/加時、到點響鈴 | 無 |
| 抽籤問答 | quiz.html | 抽人→答對答錯累積、優先抽少被抽者、即時記分榜 | 無 |
| 作業清點 | homework.html | 清點前一天派的作業＋聯絡簿、一頁全覽、未交→已交→要訂正→完成（完成即消失） | 讀 contactbook.json |
| 小組計分 | groups.html | 2–8 組（連號/蛇形/手動）、±1/±5、領先亮👑 | 無 |

共用資產：`assets/css/projection.css`（工具外殼）、`assets/css/style.css`（工作台）、`assets/js/tool.js`（`Tool.*`：座號守門/HUD自動隱藏/全螢幕/提示音/localStorage/洗牌）、`assets/js/common.js`（座號解析）。

資料來源（同源讀班網 `class-website/data/`）：`contactbook.json`（聯絡簿→電子看板/作業清點）、`schedule.json`（課表→今日課表/鐘聲）、
`countdown.json`（節慶倒數）、`class-rules.json`（鐘聲字幕的準備動作＝`daily.sop`）、`lunch.json`＋`weeks.json`（午餐值週生・依週輪替）。

⚠️ **`lunch.json` 帶去識別化姓名，依硬規則 2「本站不留學生姓名」一律只在畫面上顯示、不寫 localStorage**
（老師 2026-09-06 確認）。代價＝**斷網時午餐值週生不顯示**，其餘看板功能照常；
這是本站唯一「不套用漸進增強快取」的資料源，其餘（聯絡簿／課表／節慶／班規）照舊先畫快取再 fetch。

## Phase 2（設計 v4 定案；**2-A 已完工上線 2026-09-06**，其餘尚未動工）

**主題：課堂資料採集與回寫**——把老師在教室點的每一下寫進 Notion，
可量化的部分機器收，需要描述情境的輔導才用一句話交給 AI。

📐 **完整設計正本：[設計計畫_課堂資料採集Phase2.md](設計計畫_課堂資料採集Phase2.md)（v4 定案版）**
——資料契約、班規對照表、風險對策、路線圖與踩雷檢查表都在那裡，動工前必讀，本節只留摘要。

### 核心缺口
Phase 1 六支工具全部停在 L1（只寫 localStorage），**class-manager 沒有任何寫入路徑**。
先接管線，否則做再多工具都只是本機檔。

### 定案的五件事
1. **回寫管線＝複用既有 `submit_task`**（Apps Script 代理，token 不出 Script Properties），
   新增路由 **R18「批次事件包」**（任務原文 `#CM-EVENTS v1` ＋ JSON），排程 Agent 機械入庫。
   **後端零改動、不碰班網、延遲 ≤30 分。**
2. **常規檢核台 `routine.html`**（核心）：直接以班網公開的 `data/class-rules.json` 為骨架
   ——`cards` 10 張班規當事件目錄、`daily` 8 時段當分頁、**點完當場投影出 `fix` 修復方式給學生看**。
   打掃檢核＝其「環境晨掃」分頁（✓／△／✗ 三態，對上 class-bank 的 `基準＋支援−缺席`）。
3. **晨間報到板 `checkin.html`**：**只管上學到校**（上課遲到走常規檢核台的課堂分頁）。
   學生自己點，**機器只產生遲到候選、老師確認才成立**；不必開新 Notion 表，走紀錄庫。
   附帶好處：可機械算出班規①「一整週沒遲到 +10」，這個獎目前完全沒在發。
4. **座位加分板 `seatboard.html`**：版型＝班網座位表（需純座號版 JSON）；
   **只在 `teacher==="導師"` 的節次啟用**（科任課不開）；**依課表每節自動重置**，
   該節結束才把有次數的學生結成「舉手回答」事件。
   **輕點＝計次（不入帳，投影顯示當節次數供師生核對）**、**長按＝開班規選單才動錢**。
5. **唯一動錢的路徑是班規**。抽問／舉手一律「不論次數，每生每科每天一筆」，
   事件描述固定寫「座號N 在○○課舉手回答」，次數存欄位。
   （原設計每次入帳會讓單一學生一週拿到 +50 幣，商店券才 30–60 幣，班級經濟會崩。）

### 2026-09-06 追加定案（v4 四項）
- **打掃檢核逐人不逐組**：紙本《115班級內外掃區個人打掃檢核表 v3》每人都有自己的責任範圍，
  工具要點得到人、也顯示得出「你負責哪一塊」。**衛生監督人分三區三人各點各的**
  （內掃 23／外掃・圖書室 16／外掃・樓梯 19），監督生只產生候選、老師覆核才送出。
  資料走 **Notion 補「座號」「個人責任範圍」兩欄 → `duties-seats.json`**，
  **不把紙本抄成本站靜態表**（換座位就會變成會說謊的第二正本）。
- **語音廣播與上下課鐘聲一律配字幕**：`speak()` 接上現有 `.toast`，時長跟句長走，
  鐘聲字幕帶出「這節是什麼課、要準備什麼」（文案取 `class-rules.json` 的 `daily.sop`）。
- **節慶倒數接班網 `countdown.json`**（不另寫資料），**只顯示今天之後最近 1 筆**，
  避免學生一直想著放假；現有右下角「倒數」改稱「計時」以免混淆。
- **座位板只在導師課啟用、每節重置**；入庫仍「每天每科一筆、次數加總」
  （不動錢，細分節次只增加 Notion 筆數；要查哪一節看 localStorage 逐筆時間戳）。

### 一律例外管理
常規檢核台與報到板**都預設全班達成，老師只點例外**，多數時段 0 動作。
若要老師每個時段點 27 個人，工具一週就會被放棄。

### 階段順序
**2-A 電子看板小改版** ✅ **已完工上線 2026-09-06**（見下方「2-A 完工紀錄」）→
2-0 管線 → 2-1 **只接作業清點一支跑滿一週 PDCA** → 2-2 Notion 補欄＋班網座號版 JSON 工單 →
2-3 常規檢核台（先做晨掃＋上午課堂）→ 2-4 座位加分板 → 2-5 報到板 → 2-6 補齊其餘時段分頁。

### 2-A 完工紀錄（2026-09-06 上線並線上實測）
交付內容（`blackboard.html` 單檔，不碰管線、不新增寫入路徑）：
1. **語音廣播與上下課鐘聲一律配字幕**：`speak()`／`ring()` 共用 `subtitle()`，語音 `onend` 收；
   **無中文語音的裝置 `onerror` 會立刻回，因此字幕另設「字數÷4.5、下限 3 秒」的下限**（實作時實測抓到，原本只閃 0.6 秒）。
2. **鐘聲字幕帶「這節什麼課＋要準備什麼」**：科目取 `schedule.json`，準備動作取 `class-rules.json` 的 `daily.sop` 前兩步——**文案在 Notion，不寫死**。
3. **節慶倒數**：讀 `countdown.json`，**只顯示今天之後最近 1 筆**，當天寫「就是今天」。
4. **命名分家**：右下角分鐘計時器改稱「**計時**」，節慶那個叫「**節慶倒數**」。
5. **（追加）直式聯絡簿數字直書**：數字用 `text-combine-upright` 合併成一格、字母 `text-orientation:upright`、
   範圍號（`-` `–` `—` `~` `～`）旋轉 90° 成直線——整行同一個方向，不再只有數字橫躺。
6. **（追加）午餐值週生＋日期加星期與週次**：讀 `lunch.json`＋`weeks.json`，輪次＝`(週次-1) % 完整輪替週數 + 1`（**與班網同一條規則**），
   五個崗位各一張小卡；日期主行寫星期、次行寫「四上第N週」。

驗收證據：1600×900 stage `scrollHeight`=900 未溢出；**390／768 皆 `scrollWidth == innerWidth`**（全站 RWD）；
節慶倒數讀到「中秋節連假 還有 19 天」；SOP 摘要在早晨／上課／午餐三時段都取到正確文案；
午餐值週生取到第 1 輪五人且 `localStorage` 無 lunch 快取。
文件同步：Notion 使用說明「🤖 交辦、儀表板與維運」**新增第 7 條**、更新紀錄補 09-06 沿革、`docs/老師操作手冊.md`。

### 動工前確認：已全部完成（2026-09-06）
§4.3「時段→班規對照表」老師已看過確認；紀錄庫「次數」欄定案為**新增 `次數` number 選填欄**。
**零待確認，可直接從 2-A 開工。**

### 跨 repo 相依（各自另開工單，不在本 repo 做）
- **Notion**：「🧹 班級工作分配」補「**座號**」與「**個人責任範圍**」兩欄
  （現行只到組層級，紙本檢核表的個人責任範圍在後台還沒有欄位）。
- **class-website**：sync 多輸出純座號版 `duties-seats.json`（含個人責任範圍與三區監督人）
  ／`seating-seats.json`（純座號 grid）
  （現行 `duties.json`／`seating.json` 只有去識別化姓名、無座號，而本站一字不存姓名）。
- **藍圖／排程**：R18 上線會觸發**第五同步**（改 `07_排程Agent_routine_prompt.md`
  ＋ `RemoteTrigger` 更新雲端兩條 routine，回讀確認 `enabled`／`cron` 未被動到）。

### Phase 1 完工當時的其餘候選
- **語音廣播字幕＋節慶倒數**（原為候選）→ **已納入 Phase 2 的 2-A**，見上。
- **天氣看板**：需外部 API＝違反離線硬規則，**不做**。
- **第三站 study-orbit**（自學星圖）：另立藍圖。

## 動工提醒
- 改配色/版面只改 `assets/css/{projection,style}.css` 的 `:root` 與 `body::before`，六支工具用同一組 CSS 變數，不要在個別工具硬寫色。
- 任何新工具沿用 `Tool.requireSeats(stageEl)` 取座號、`projection.css` 外殼、加 @media 斷點、加 `noindex`。
- 完工定義：push 後 `curl` 確認 200、桌機 1600×900 無溢出、390 寬無橫捲、同步 `docs/老師操作手冊.md`（班級鐵則 8）。
