# class-manager 設計藍圖與路線圖

> 教師支援系統**第二站**：班級管理系統（老師在教室投影用的課堂工具）。
> 上位規則：`Project/AGENTS.md`、`班級事務/CLAUDE.md`、本站 `class-manager/CLAUDE.md`。
> 三站架構：班網 class-website（家長學生・唯讀公開）／**班級管理系統 class-manager（老師・教室投影・本站）**／自學星圖 study-orbit（學生・自學，尚未建）。三站獨立 repo，共用 Notion 後台。

## 設計原則（動工前必守）
- **純靜態、零相依、斷網照常**：無 build、無 npm、無外部 CDN/字型。教室會斷網，斷網時全部工具照常可用。
- **連網只做漸進增強**：讀「同域名姊妹站」班網的公開 JSON（同源、免 token、不改班網檔）；先畫本機快取→再抓雲端→離線用快取。
- **只存座號、不存姓名**：座號在工作台設定（localStorage）；姓名/查詢碼/個資一個字都不寫。
- **寫入分級**：L1 即時只寫 localStorage（Phase 1 全部工具都在此級，不碰網路寫入）；L2 事件即時送出（尚未做）。
- **視覺基準**：參照「智慧教室儀表板 2.0」（apaulliao.github.io/schooltool）——近黑星空底、玻璃圓角卡片、彩色膠囊工具列。色票正本 `assets/css/{projection,style}.css` 的 `:root`。
- **投影版面**：1600×900、主要內容 ≥40px、深色高對比；**全站 RWD**（@media 900/560，390 寬須無橫捲）。

## Phase 1（已完工・2026-09-04）
六支課堂工具全部上線，桌機投影＋手機皆可用。

| 工具 | 檔案 | 重點 | 連網 |
|---|---|---|---|
| 電子看板 | blackboard.html | 直式聯絡簿（數字直書）・值日生輪值・**午餐值週生**・今日課表・月相時鐘・日期含星期與週次・**節慶倒數**・語音廣播（**配字幕**）・計時・上下課鐘聲（**配字幕**） | 讀 contactbook／schedule／countdown／class-rules／lunch／weeks.json |
| 抽籤 | draw.html | 不重複、每人限中一次、每日重置 | 無 |
| 計時 | timer.html | 大字倒數＋碼錶、預設/自訂、暫停/加時、到點響鈴 | 無 |
| 抽籤問答 | quiz.html | 抽人→答對答錯累積、優先抽少被抽者、即時記分榜 | 無 |
| 作業清點 | homework.html（**2026-09-07 併入 `routine.html` 分頁，本檔刪除**） | 清點前一天派的作業＋聯絡簿；**2026-09-08 起座位表模式**（一格＝一位學生，格內只列未完成的作業，交齊變暗；可切單項模式）。每筆作業鍵＝「派出日期｜名稱」，聯絡簿每天自動換新列；未交完的進「⏳ 過去未完成」區、不再結算 | 讀 contactbook.json |
| 小組計分 | groups.html | 2–8 組（連號/蛇形/手動）、±1/±5、領先亮👑 | 無 |

共用資產：`assets/css/projection.css`（工具外殼）、`assets/css/style.css`（工作台）、`assets/js/tool.js`（`Tool.*`：座號守門/HUD自動隱藏/全螢幕/提示音/localStorage/洗牌）、`assets/js/common.js`（座號解析）。

資料來源（同源讀班網 `class-website/data/`）：`contactbook.json`（聯絡簿→電子看板/作業清點）、`schedule.json`（課表→今日課表/鐘聲）、
`countdown.json`（節慶倒數）、`class-rules.json`（鐘聲字幕的準備動作＝`daily.sop`）、`lunch.json`＋`weeks.json`（午餐值週生・依週輪替）。

⚠️ **`lunch.json` 帶去識別化姓名，依硬規則 2「本站不留學生姓名」一律只在畫面上顯示、不寫 localStorage**
（老師 2026-09-06 確認）。代價＝**斷網時午餐值週生不顯示**，其餘看板功能照常；
這是本站唯一「不套用漸進增強快取」的資料源，其餘（聯絡簿／課表／節慶／班規）照舊先畫快取再 fetch。

## Phase 2（設計 v4 定案；**2-A、2-0、2-1～2-3 已完工上線 2026-09-06**；2-4 座位加分板已於 Phase 3-1 併進電子白板）

**主題：課堂資料採集與回寫**——把老師在教室點的每一下寫進 Notion，
可量化的部分機器收，需要描述情境的輔導才用一句話交給 AI。

📐 **完整設計正本：[設計計畫_課堂資料採集Phase2.md](設計計畫_課堂資料採集Phase2.md)（v4 定案版）**
——資料契約、班規對照表、風險對策、路線圖與踩雷檢查表都在那裡，動工前必讀，本節只留摘要。

### 核心缺口
Phase 1 六支工具全部停在 L1（只寫 localStorage），**class-manager 沒有任何寫入路徑**。
先接管線，否則做再多工具都只是本機檔。

### 定案的五件事
1. **回寫管線＝複用既有 `submit_task`**（Apps Script 代理，token 不出 Script Properties），
   新增路由 **R18「批次事件包」**（任務原文 `#CM-EVENTS v1` ＋ JSON），排程 Agent 機械入庫。
   **後端零改動、不碰班網、延遲 ≤30 分。**
2. **常規檢核台 `routine.html`**（核心）：直接以班網公開的 `data/class-rules.json` 為骨架
   ——`cards` 10 張班規當事件目錄、`daily` 8 時段當分頁、**點完當場投影出 `fix` 修復方式給學生看**。
   打掃檢核＝其「環境晨掃」分頁（✓／△／✗ 三態，對上 class-bank 的 `基準＋支援−缺席`）。
3. **晨間報到板 `checkin.html`**：**只管上學到校**（上課遲到走常規檢核台的課堂分頁）。
   學生自己點，**機器只產生遲到候選、老師確認才成立**；不必開新 Notion 表，走紀錄庫。
   附帶好處：可機械算出班規①「一整週沒遲到 +10」，這個獎目前完全沒在發。
4. **座位加分板 `seatboard.html`**：版型＝班網座位表（需純座號版 JSON）；
   **只在 `teacher==="導師"` 的節次啟用**（科任課不開）；**依課表每節自動重置**，
   該節結束才把有次數的學生結成「舉手回答」事件。
   **輕點＝計次（不入帳，投影顯示當節次數供師生核對）**、**長按＝開班規選單才動錢**。
5. **唯一動錢的路徑是班規**。抽問／舉手一律「不論次數，每生每科每天一筆」，
   事件描述固定寫「座號N 在○○課舉手回答」，次數存欄位。
   （原設計每次入帳會讓單一學生一週拿到 +50 幣，商店券才 30–60 幣，班級經濟會崩。）

### 2026-09-06 追加定案（v4 四項）
- **打掃檢核逐人不逐組**：紙本《115班級內外掃區個人打掃檢核表 v3》每人都有自己的責任範圍，
  工具要點得到人、也顯示得出「你負責哪一塊」。**衛生監督人分三區三人各點各的**
  （內掃 23／外掃・圖書室 16／外掃・樓梯 19），監督生只產生候選、老師覆核才送出。
  資料走 **Notion 補「座號」「個人責任範圍」兩欄 → `duties-seats.json`**，
  **不把紙本抄成本站靜態表**（換座位就會變成會說謊的第二正本）。
- **語音廣播與上下課鐘聲一律配字幕**：`speak()` 接上現有 `.toast`，時長跟句長走，
  鐘聲字幕帶出「這節是什麼課、要準備什麼」（文案取 `class-rules.json` 的 `daily.sop`）。
- **節慶倒數接班網 `countdown.json`**（不另寫資料），**只顯示今天之後最近 1 筆**，
  避免學生一直想著放假；現有右下角「倒數」改稱「計時」以免混淆。
- **座位板只在導師課啟用、每節重置**；入庫仍「每天每科一筆、次數加總」
  （不動錢，細分節次只增加 Notion 筆數；要查哪一節看 localStorage 逐筆時間戳）。

### 一律例外管理
常規檢核台與報到板**都預設全班達成，老師只點例外**，多數時段 0 動作。
若要老師每個時段點 27 個人，工具一週就會被放棄。

### 階段順序
**2-A** ✅ → **2-0 管線** ✅ → **2-1 作業清點接線** ✅ → **2-2 Notion 補欄＋班網座號版 JSON** ✅ →
**2-3 常規檢核台（晨掃＋上午課堂）** ✅ → **模擬驗收與制度調整** ✅ —— 以上全部 2026-09-06 →
**下一步：① 週結規則寫進 class-bank ② 第一次真的收班送出（老師在場）③ 才做 2-4 座位加分板**
→ 2-5 報到板 → 2-6 補齊其餘時段分頁。

⚠️ **兩件事沒做完，往下走之前要先補**：
① **一包都還沒真的送出過**（收班需要老師口令），下次收班要老師在場走完
　送出 → 收件匣 → 排程入庫 → 回讀紀錄庫；
② 2-1 的「**實際用滿一週**」PDCA 還沒跑（收班不重複入帳／紀錄庫沒被灌成噪音／週結金額正確）。

### 2026-09-06 模擬驗收與制度調整（同日下午）

**上線後先跑一輪零寫入的模擬驗收**（現場測試會打斷上課、錯了會真的動到學生的錢），
用真的 `events.js` 加線上真資料跑 27 人五天。完整報告：`docs/驗收報告_Phase2第一階段_模擬.md`。

**找到七個問題，修掉六個**（第七個是通膨，由老師裁示改設計）：

| # | 問題 | 修法 |
|---|---|---|
| 1 | 晨掃 △ 第一次就扣 −5，**違反既有制度**（1～2 次不扣幣只補做、3 次以上才 −5） | 改成 tally「打掃未達標」金幣 0，累計判斷交週結 |
| 2 | 晨掃 tally 帶批次號 → 同一天重新結算變兩列 → **週結多扣一次出勤** | 改用 `dedupe:'day'`；判準是「一天一列嗎」而不是 src，抽問那種計次式仍保留批次號 |
| 3 | 作業「完成 +5」→ 每人每週 +25（比職務週薪還多），**1.78 倍通膨** | 改成 tally「作業完成」只計次，週結看全週一次給 |
| 4 | ⑩重大安全事件 −200 與「打斷發言 −5」並排、點了就記 | 幣值 ≥10 一律二次確認 |
| 5 | `coin` 是 U+2212 字串寫進 number 欄會**靜默失敗** | R18 規格補「先 replace 再轉數字，轉不成記 E07 不要寫 0」 |
| 6 | 無 `subj` 的 tally 沒有事件描述與類別規則（週結靠字串撈） | 補四種 tally 的撈取鍵與類別，另補「名冊只查一次」（省查詢配額） |

**同日制度調整（老師裁示）**：

- **晨掃分三頁**（內掃區 23號／外掃區・圖書室 16號／外掃區・樓梯 19號），監督同學點自己那頁。
  **頁籤名不寫死**——取該監督各組組別名的共同字自動生成，改 Notion 掃區分組會跟著改。
- **本分項只記次數**：`DUTY_PAID` = ③good[0] 打掃本分、⑦good[1] 幹部職務本分（週薪已支付，
  再入帳就是同一件事付兩次）。**只限有工作薪水撐著的這兩張卡**——一刀切會把②有禮貌、⑤先聽完
  再回應那些沒有薪水對應的正向獎勵平白取消。⚠️ 兩張卡不在「上午課堂」時段，**要等 2-6 才有作用點**。
- ⑤「被記 2 個 ×」標明是**升級處分**（同一行為第 3 次才用），避免與個別扣點重複。
- 🔴 **金幣公式收成一條**：`正負向 × 程度 × 5`，**亮點類不再 ×10**。舊公式只算得出 10／20／30，
  對不上班規卡在用的 +5／+10／+15。「亮點」自此只是分類，不是第二套算錢方式。
- **幹部額外獎金不分名次一律 +15**（亮點・程度 3）、**進步獎 +20 → +5**（亮點・程度 1）。
  前者原本「第一名 30」與 R01 榮譽競賽規則的「幹部組 +10」**兩份規則對同一件事給不同金額**，一併統一。

改版後同一組亂數重跑：每人每週 **38.9 幣**（只有固定薪水是 37.0），通膨解除且沒變成通縮，商店價位不必動。

### 2-1～2-3 完工紀錄（2026-09-06 上線）

- **2-1**：`homework.html` 加「📤 結算到待送」。未交→④bad[0]、要訂正→④bad[1]、
  完成→④good[0]、已交不記（老師拍板）；幣值原封轉抄班規，讀不到就鎖住結算鈕。
  順手修掉 2-0 管線四個坑：`sig()` 沒帶 kind（未交 −5 與完成 +5 會併成一列）、
  rule 類 id 帶批次號（重送會**重複發錢**）、合併只留第一筆 note、工作台顯示「++5 幣」。
- **2-2**：Notion「🧹 班級工作分配」新增 `個人責任範圍`／`監督座號` 兩欄
  （**原本就有「成員座號」**，計畫寫的補座號欄不必做），依老師的紙本檢核表 PDF 填完 12 列並回讀；
  班網 sync 多輸出 `duties-seats.json`／`seating-seats.json`（純座號零姓名，含護欄）。
- **2-3**：`routine.html`。晨掃分頁四態 ✓△✗＋、人卡副標＝個人責任範圍、監督生下拉、
  本週總覽取代列印紙本；課堂分頁用座位版型、點座號選班規、當場投影修復方式、遲到問分鐘。
  兩個分頁用兩個 tool 名（`cleanup`／`routine`），否則晨掃重新結算會清掉課堂的即時事件。

### 2-0 完工紀錄（2026-09-06 上線）

T0 回寫管線接通，**課堂工具的加減分終於有路可以進 Notion**（Phase 1 的核心缺口補上）。

交付內容：
1. **`assets/js/events.js`**：事件佇列 `CMEvents`。同一位學生×同一天×同一工具×同一類**自動合併成一列並記次數**；
   `id`＝`工具-日期-b批次-s座號-特徵`＝防重複鍵；payload 超過 1800 字自動切包（代理 `submit_task` 上限 2000 字），
   envelope 帶 `part`／`parts`，各包獨立入庫。**工具頁只 push，不含送出程式碼、不持有口令**（設計計畫 §3.4）。
2. **工作台「今日待送」面板**（`index.html`）：合併後清單／預覽送出內容／收班送出／全部丟棄。
   口令 `type=password`、**永不寫 localStorage**；**送出成功才清、失敗整批留本機**。
3. **R18 路由**：`05_任務路由登記簿_v1.md`（v1.6，排最前，命中＝首行 `#CM-EVENTS`）＋
   `07_排程Agent_routine_prompt.md` §四之四。**第五同步已做**：雲端 routine `trig_01XdMEGw7GADMsu3r7jNPdus`
   已更新並回讀（`enabled:true`／cron `0 0-9 * * 1-5`／4 條 mcp_connections 未動）。
4. **Notion 紀錄庫新增兩欄**：`次數`（number）＋ **`事件id`（rich_text）**。後者計畫沒寫但防重複必需——
   沒有它就無法「查同 id 是否已入庫」，U44 那道關卡形同虛設。
5. **放學提醒**（同日老師追加）：放學時間到還有待送 → 工作台橘色橫幅（現在送出／今天不再提醒）、
   電子看板左下膠囊「⏰ N 筆未送出」（**只顯示筆數，不含姓名與內容，可投影**）。
   時間依星期：**週三、五 12:40／週一、二、四 16:00**（週末沿用 16:00），正本＝`events.js` 的 `REMIND_AT` 表。

三個關鍵設計：
- **前端不算錢**：`src:"rule"` 的 coin／level 只是原封轉抄班規，**由 R18 回讀 `class-rules.json` 核對才入帳**
  （不符該筆標 E07、其餘照寫）；`src:"tally"` 一律金幣 0 只記次數（§6 金幣通膨）。
- **批次號只在送出成功後 +1**：斷網重試沿用同號（同 id → R18 去重），同天送第二次是 b2，次數不互相蓋掉。
- **全自動送出做不到也不該做**：待送在瀏覽器 localStorage、排程讀不到，口令又永不落地；
  **入庫端本來就已自動**（雲端 routine 每整點）。所以只做提醒，送出永遠是老師按的那一下。

驗收證據：合併與切包有 node 單元測試（40 筆→3 包、40 個 id 全唯一）；線上實測口令錯誤時 3 筆全留本機；
放學提醒橫幅／膠囊顯示與「今天不再提醒」持久化；1600×900 膠囊不壓 HUD（chipRight 314 < hudLeft 400）、stage 未溢出；390 無橫捲。
文件同步：Notion 使用說明「🤖 交辦、儀表板與維運」**新增第 8 條**、更新紀錄補兩則 09-06 沿革、`docs/老師操作手冊.md`、設計計畫 §7A。

⚠️ **PDCA 的 Check 缺口**：端到端只驗到「代理連得上、口令錯時整批留本機」，**沒有真的送出過一包**（要老師口令、會真寫進收件匣）。
**2-1 第一次收班要老師在場一起看**：送出 → 收件匣出現 → 排程入庫 → 回讀紀錄庫欄位齊全。

⚠️ **目前按收班會是 0 筆**：2-0 只做管線，**六支工具一支都還沒接**（`grep CMEvents` 在 quiz／homework／groups／draw 皆為 0）。
老師期待的「一整天所有紀錄一次送出」要等 2-1～2-5 逐支接線後才成立。

### 2-A 完工紀錄（2026-09-06 上線並線上實測）
交付內容（`blackboard.html` 單檔，不碰管線、不新增寫入路徑）：
1. **語音廣播與上下課鐘聲一律配字幕**：`speak()`／`ring()` 共用 `subtitle()`，語音 `onend` 收；
   **無中文語音的裝置 `onerror` 會立刻回，因此字幕另設「字數÷4.5、下限 3 秒」的下限**（實作時實測抓到，原本只閃 0.6 秒）。
2. **鐘聲字幕帶「這節什麼課＋要準備什麼」**：科目取 `schedule.json`，準備動作取 `class-rules.json` 的 `daily.sop` 前兩步——**文案在 Notion，不寫死**。
3. **節慶倒數**：讀 `countdown.json`，**只顯示今天之後最近 1 筆**，當天寫「就是今天」。
4. **命名分家**：右下角分鐘計時器改稱「**計時**」，節慶那個叫「**節慶倒數**」。
5. **（追加）直式聯絡簿數字直書**：數字用 `text-combine-upright` 合併成一格、字母 `text-orientation:upright`、
   範圍號（`-` `–` `—` `~` `～`）旋轉 90° 成直線——整行同一個方向，不再只有數字橫躺。
6. **（追加）午餐值週生＋日期加星期與週次**：讀 `lunch.json`＋`weeks.json`，輪次＝`(週次-1) % 完整輪替週數 + 1`（**與班網同一條規則**），
   五個崗位各一張小卡；日期主行寫星期、次行寫「四上第N週」。

驗收證據：1600×900 stage `scrollHeight`=900 未溢出；**390／768 皆 `scrollWidth == innerWidth`**（全站 RWD）；
節慶倒數讀到「中秋節連假 還有 19 天」；SOP 摘要在早晨／上課／午餐三時段都取到正確文案；
午餐值週生取到第 1 輪五人且 `localStorage` 無 lunch 快取。
文件同步：Notion 使用說明「🤖 交辦、儀表板與維運」**新增第 7 條**、更新紀錄補 09-06 沿革、`docs/老師操作手冊.md`。

### 動工前確認：已全部完成（2026-09-06）
§4.3「時段→班規對照表」老師已看過確認；紀錄庫「次數」欄定案為**新增 `次數` number 選填欄**。
**零待確認。2-A、2-0 已完工，下一步＝2-1。**

### 跨 repo 相依（各自另開工單，不在本 repo 做）
- **Notion**：「🧹 班級工作分配」補「**座號**」與「**個人責任範圍**」兩欄
  （現行只到組層級，紙本檢核表的個人責任範圍在後台還沒有欄位）。
- **class-website**：sync 多輸出純座號版 `duties-seats.json`（含個人責任範圍與三區監督人）
  ／`seating-seats.json`（純座號 grid）
  （現行 `duties.json`／`seating.json` 只有去識別化姓名、無座號，而本站一字不存姓名）。
- **藍圖／排程**：R18 上線會觸發**第五同步**（改 `07_排程Agent_routine_prompt.md`
  ＋ `RemoteTrigger` 更新雲端兩條 routine，回讀確認 `enabled`／`cron` 未被動到）。

### Phase 1 完工當時的其餘候選
- **語音廣播字幕＋節慶倒數**（原為候選）→ **已納入 Phase 2 的 2-A**，見上。
- **天氣看板**：需外部 API＝違反離線硬規則，**不做**。
- **第三站 study-orbit**（自學星圖）：另立藍圖。

## 動工提醒
- 改配色/版面只改 `assets/css/{projection,style}.css` 的 `:root` 與 `body::before`，六支工具用同一組 CSS 變數，不要在個別工具硬寫色。
- 任何新工具沿用 `Tool.requireSeats(stageEl)` 取座號、`projection.css` 外殼、加 @media 斷點、加 `noindex`。
- 完工定義：push 後 `curl` 確認 200、桌機 1600×900 無溢出、390 寬無橫捲、同步 `docs/老師操作手冊.md`（班級鐵則 8）。

## Phase 3（教師工作台重整・2026-09-06 開工）

老師的裁示：工作台切成**「分班級流程」的課堂工具**與**「依時序」的工作檢核**兩區，
今日待送與班級座號另闢**教師專區**（學生誤按會弄髒資料）。分兩輪做，先白板、再檢核台。

### 3-1 電子白板整併（✅ 已完工上線 2026-09-06）

**六支工具收成三支**：抽籤問答／計時／小組計分／座位加分板全部併進電子白板，
**「抽籤」取消**（quiz 的「抽一位」就是抽籤，功能重疊）。`draw/quiz/groups/timer.html` 四檔刪除，
localStorage 鍵沿用（`classManager.groups.v1` 等），所以老師既有的分組與分數不會歸零。

交付內容（新檔 `assets/js/board.js` ＋ 改寫 `blackboard.html`）：

1. **依日課表自動換內容**：早晨入班→聯絡簿／晨掃→掃區流程／早自修→Morning Launch（讀 `morning-launch.json`
   當天 dow 的主題與四張卡）／上課節→本節重點（`lessons.json` 依日期＋科目對到單元的 `points`）／
   下課→班規①⑥＋`flows` 的「進教室」／午餐潔牙放學→該時段 SOP。
   **文案全部在 Notion**，本站只讀不寫。右側常規欄取 `class-rules.json` 的 `daily`（sop 步驟＋expect）。
2. **座位加分板**（原 2-4，提前做）：版型讀 `seating-seats.json`（純座號）。
   **輕點＝當節計次（只在 `teacher==="導師"` 的節次啟用）**、**長按／右鍵＝班規選單**（與 routine.js 同一套判準：
   本分項只記次數、幣值 ≥10 二次確認、遲到問分鐘、當場投影修復方式）。**每節自動重置。**
3. **結束課程**：把該節的發表次數與抽問結果結成 `src:'tally'`（金幣 0）事件進待送，
   合併後＝「每生每科每天一列＋次數」，另存一筆當節活動紀錄（`Board.log()`，供 3-2 的教師專區顯示）。
   **老師忘了按也不會掉**：`watchPeriod` 每 5 秒比對節次，換節時自動結算上一節（實測驗過）。
4. **白板畫記**：主區上疊一層 canvas，白／黃／粉三色、橡皮擦、清空、收起。
5. **減少分心**：`Board.view` 逐項開關（月相／節慶／午餐／值日生／課表／時鐘／常規欄），
   HUD「精簡」一鍵全關。⚠️ 課表那項要**同時**擋掉舊的 `state.opt.sched` 迴圈，否則 250ms 後又被打開（實測抓到）。

**實測踩到三個坑**（都已修，寫在程式註解裡）：
① `class-rules.json` 是物件不是陣列，對整包 `filter` 會丟例外 → 整個 render 中斷、白板全空；
② `.notes` 自己設了 `display:flex`，蓋掉瀏覽器對 `[hidden]` 的 `display:none` → 聯絡簿與白板內容疊在一起；
③ 抽籤滾動的最後一筆會與重畫擦身而過，號碼停在灰色像沒抽完 → 重畫後再寫一次。

驗收證據：1600×900 六個模式 stage `scrollHeight` 皆 900 未溢出；390 寬六個模式 `scrollWidth == innerWidth`；
座位板讀到真實座位表 27 格；假課表（導師・數學）輕點 3 次 → 結束課程 → `CMEvents.merged()`
得兩列（s7 count 2、s13 count 1，`src:'tally'`、金幣 0）＋活動紀錄一筆；換節自動結算亦驗過。

### 3-2 工作檢核時序化＋教師專區（✅ 已完工上線 2026-09-07）

- **工作檢核**依時序收成一頁：到校簽到 → 打掃工作檢核（分三區）→ 作業清點檢核 → 午餐工作檢核 → 潔牙檢核。
  `homework.html` **併成檢核台的一個分頁**（老師 2026-09-06 拍板），到校簽到＝原 2-5 報到板。
- **教師專區** `teacher.html`：今日待送、班級座號、當節活動紀錄搬過去，首頁只留小入口，
  避免學生在投影機前誤按（口令仍只在送出時輸入，永不落地）。

**開工前三件事已由老師拍板（2026-09-06）**：

| # | 問題 | 老師的答覆 |
|---|---|---|
| 1 | 午餐／潔牙的資料來源 | **午餐**＝比照班網「工作分配－午餐工作」（`lunch.json`：午餐長／打飯班固定崗＋搬餐桶輪值），**介面沿用打掃工作檢核表**（座號卡循環 ✓／△／✗／＋）；**潔牙**＝比照**作業清點檢核**（只要分「有潔牙／沒潔牙」，不需 Notion 開新欄位）|
| 2 | 五站順序與完成判準 | 順序固定 **到校簽到 → 打掃三區 → 作業清點 → 午餐工作 → 潔牙**；**五站一律逐生勾**（不做整區打勾）|
| 3 | 到校簽到 | **座位表輕點**（與電子白板同一套手勢），不用原 2-5 報到板 |

**衍生兩題也已拍板（2026-09-06）**：

- **A. 午餐不做「△ 未達標」**——午餐工作檢核只做 **✓ 到位／✗ 未到／＋ 臨時支援** 三態。
  理由：午餐工作當場就看得到成果，用不到事後累計；③ 是打掃專用班規，不為午餐另開一條。
- **B. 潔牙不另開帳**——併進既有「**班級常規獎勵**」的例外管理：
  該天沒潔牙＝該生當天常規未達成，那天的 +1 不給、全勤獎也沒有。**不扣幣、不記班規。**

**因此 3-2 要新增的 tally `act`（三個）**：`午餐支援`／`午餐缺席`／`常規未達成`。

⚠️ **這三個字串要進 `07_排程Agent_routine_prompt.md` 兩個地方，且要走第五同步**
（`RemoteTrigger` 更新雲端 routine，不是只改本機檔）：
1. 「課堂工具會送出的 tally」撈取鍵表加三列；
2. **類別對照要一起擴充**——現行規則是「含『打掃』→生活技能／含『作業』→作業／其餘有 `subj`→課堂表現」，
   這三個新字串**一條都不符合**（沒有 `subj`），不補就會寫出類別空白的紀錄。
   建議：含「午餐」→ 生活技能；`常規未達成` → 生活指導。
**排在 3-2 實際送得出這些事件的那一刻做，不要提前**——鍵先進雲端但沒人送，只是多一份要維護的漂移。

#### 交付內容（2026-09-07）

1. **`routine.html`／`assets/js/routine.js` 改寫成五站**（原「常規檢核台」更名**工作檢核台**）：
   `arrive`／`cleanup`／`homework`／`lunch`／`teeth` **五個 tool 名各自獨立**——每站的結算都是
   `clearTool` 後整批重 push，共用一個 tool 名會讓 A 站的重新結算把 B 站的結果一起清掉。
   - **到校簽到**＝座位表輕點（✓到校→⏰遲到→✗未到）。遲到記班規①bad[0]；
     **「未到」刻意不送出**——請假不是偏差行為，也不是週結公式的輸入，送出去只是紀錄庫多一列沒人用的資料。
   - **午餐**讀新的 `lunch-seats.json`，固定崗直接列座號，值週生依 weeks.json 算本週輪次
     （`((本學期週次-1) % 完整輪替週數)+1`，與班網 about.js 同一條公式，**模數不寫死**）。三態 ✓／✗／＋。
   - **潔牙**兩態，沒潔牙送 `常規未達成`。
2. **`homework.html` 刪除**，內容成為檢核台第三個分頁；**沿用原 `classManager.homework.v2` store**，
   老師當天已經點過的清點不會因為併頁歸零。
3. **`teacher.html`＋`assets/js/teacher.js` 新增**：今日待送與收班送出、當節活動紀錄（讀 `Board.log`）、
   班級座號設定全部搬進來；`index.html` 只留三個入口與待送筆數。
4. **`class-website` 多輸出 `lunch-seats.json`**（純座號、零姓名，走既有的「座號版不得含姓名」護欄）。
   午餐輪值改成先算座號版 `rotationSeats`、再映射成遮罩姓名版——**兩份由同一次計算產出**，
   分開算兩次就是兩份會各自漂移的正本。
5. **三個新 tally 已進 `07_排程Agent_routine_prompt.md` 並走完第五同步**（見下）。

#### 「上午課堂」分頁移除（2026-09-07）

電子白板的座位板**長按（右鍵）就是同一套班規選單**，而且不限導師課、白板才是上課中投影的那一頁。
檢核台再留一個入口，等於同一件事有兩份正本。舊分頁的 `routine` tool 名保留在 events.js 的
標籤表裡（標「3-2 已移除，舊資料才會出現」），老師機器上若還有沒送出的舊事件仍看得懂。

#### 第五同步（2026-09-07 完成）

`RemoteTrigger action=update trig_01XdMEGw7GADMsu3r7jNPdus`，外科式改三處：
tally 表由四種增為七種（＋`午餐支援`／`午餐缺席`／`常規未達成`，來源欄一併改稱工作檢核台）、
類別對照補「含『午餐』→生活技能」「`常規未達成`→生活指導」並加一條
「**判不出來標『需人工』，不要留空**」（類別空白不報錯，但那列在班網與週結都對不到分類）、
每輪自查加一條「R18 的 tally 類別沒有留空」。
回讀確認：`enabled: true`、cron `0 0-9 * * 1-5`、4 條 MCP 連線都未被動到。

#### 驗收證據（2026-09-07 線上正式站實測）

- 五站都讀到真實資料：打掃 12 張卡／午餐 6 張卡（5 固定崗＋值週生）／作業 4 份 108 chips／潔牙 27 chips／
  簽到 27 座號＋3 空位；午餐值週生對到 **第 2 輪・四上第2週(9/7-9/11)**，座號與 `lunch-seats.json` 逐格相符。
- 模擬點選後五站各自結算，`CMEvents.merged()` 得：`arrive` 座號5 上學遲到 −5（rule）、
  `cleanup` 三筆 tally、`lunch` 午餐缺席／午餐支援各一、`teeth` 常規未達成一筆、`homework` 未交／要訂正／完成；
  **座號 9 標「未到」沒有產生任何事件**（符合設計）。午餐站連按兩次結算仍是 2 列（防疊加成立）。
- 版面：1600×900 五站 stage `scrollHeight` 皆 900、頁面 `scrollWidth` 1600 未溢出；
  390 寬五站 `scrollWidth == innerWidth`；`teacher.html`／`index.html` 390 寬同樣不橫捲。
- 預覽送出內容的來源名正確顯示「工作檢核台・到校簽到／打掃／作業清點／午餐／潔牙」。
- **測試資料未送出**（全程沒輸入口令），驗收後已 `CMEvents.clearAll()` 清乾淨，Notion 完全未動。

### Phase 2／3-1 遺留的 Check 缺口（2026-09-06 夜間大部分結案）

- ✅ **送出 → 收件匣 前半段已走通**（2026-09-06 老師實測）：工作台按「收班送出」後，
  兩包 `#CM-EVENTS` 確實出現在 📥 任務收件匣、狀態＝待處理。**測試資料事後已改「已取消」並手動刪除**
  （若留著，隔天 08:01 排程會真的入帳給座號 18、2 各 +5 幣——這條務必寫進之後的測試須知）。
- ✅ **收件匣 → 排程入庫 → 回讀紀錄庫 已結案**（2026-09-06 手動觸發雲端 routine 實測）：
  寫入的紀錄庫列 事件描述「在數學課舉手回答」／次數 2／金幣影響 0／正負向 ＋／
  週次「四上第1週(8/31-9/4)」／學年 115／類別 課堂表現／學生 relation 正確，**七欄全部有值且正確**；
  收件匣該列回填 路由ID R18、產出連結、執行紀錄「已入庫 1 筆／略過 0／失敗 0」、狀態已完成。
  run `cse_01SqeteU8kzBcgEUfpnotz28`。**R18 全鏈路自此為實測成立，不再是設計上成立。**
- 🐞 **同一次驗收抓到並修掉一個會靜默吃掉任務的 bug**：Agent 對收件匣「狀態」下了
  `propertyType:"status"` 的 filter（實際是 **select**），Notion 回 0 筆且不報錯，
  於是誤判空件、回報 success、任務永遠卡在待處理。**間歇性**——filter 型別是模型每輪自己猜的。
  修法已進雲端 prompt（走第五同步）：步驟 1 改成整份撈回自己過濾、不准猜 propertyType，
  並加自查「回報沒有待處理任務前先確認總筆數 > 0」。詳見 `07_排程Agent_routine_prompt.md` §三。
- ✅ **座位板「只在導師課計次」已結案**（2026-09-06）：
  老師實測非導師課輕點不計次；導師課計次、以及**忘按「結束課程」換節自動結算**
  則以假時鐘在線上正式站模擬通過（週一第一節數學計次、第二節體育 0 次、
  3 次併成 座號5×2／座號12×1 且金幣 0）。

### 2026-09-06 夜間一併修掉的兩件事

- **「預覽送出內容」原本直接倒 `#CM-EVENTS` JSON，老師看不懂** → 改成逐筆
  「座號 · 班規第幾條 · 行為 · 幾幣」＋每包金幣合計，tally 明寫「只記次數，不動金幣」；
  原始封包退到「顯示原始封包」按鈕後（`describePayloads()`／`rawPayloads()`）。
- **靜態資源版本號沒 bump，改了 JS 但瀏覽器續用舊檔**（第一次線上驗證就驗到舊版才發現）。
  **往後只要動到 `assets/js/*`，`index.html`／`blackboard.html` 的 `?v=` 一定要一起 bump。**

---

## 3-2 上線後的現場修正（2026-09-07 傍晚～夜間，藍圖 2026-09-08 補記）

> 這一批是**老師當天實跑後直接指定的改動**，當時只 commit 沒回填藍圖，2026-09-08 收工時補上。
> 教訓：手冊有同步（班級鐵則 8 顧到了），但藍圖漏記，**下一個 session 會看不到現行設計的一半**。

- **四個狀態站全部改「一格一格點名」**（`c023aa0`／`3ae4a20`，老師拍板）：
  到校／打掃／午餐／潔牙的**第 0 態一律是「還沒點」**（灰色虛線），點下去才是出席／到位／已潔牙；
  另加「✅ 全部○○」一鍵**只填還沒點的**（已標遲到／未達標的不會被蓋掉）與進度列「已點 n/N」。
  理由：原本預設全班達成，老師分不出「已檢核且達標」與「還沒檢核」，投影時學生也看不到自己被點名的那一下。
  ⚠️ 狀態編號存 `classManager.routine.v2` 的 **`sv` 欄（現行 4）——改狀態表必須推 `sv`**，
  否則舊資料會被新編號讀成別的意思（昨天的出席變今天的遲到）。
- **作業清點的欠交結轉**（`3ae4a20`）：舊項目只要還有人沒到「完成」就留在清單（`hw.carry`），
  **結轉列不進 `collect()`**——同一份作業天天結算會天天扣 −5。
- **潔牙站加「💧 含氟漱口水」**（`9f4ecc3`）：一週一次，由老師當天自己開（`st.fluorideOn`），
  三態同潔牙；沿用同一個 act `常規未達成`（排程端與週結不必新增規則），
  同一位學生兩項都沒做會**合併成一列**（次數 2、備註兩項都留、金幣 0）。
- **送 Notion 的任務標題改成看得懂**（`74d6615`）：指紋後面接一句白話摘要，老師在收件匣一眼認得出是哪一批。
- **廣播／鐘聲字幕移到畫面正中央並放大**（`9a39623`）：投影在教室後排看得到。

## Phase 3-3 電子白板＝班級電子公布欄（✅ 已完工上線 2026-09-07）

**老師拍板：打開白板就是公布欄。** 一頁只講一件事，依時段自動換；當天可切到別的模式，**隔天開頁回到公布欄**。

- 規則正本 **`assets/js/wall.js`**（白板與 `wall-demo.html` 預覽頁**共用同一支**，改規則只改那一支——
  兩份會各自漂移，示範頁與正式頁講不同的話比沒有示範頁更糟）。
- `blackboard.html` 開頁預設 `mode='wall'`（`a6f1be9`）；HUD 的「📢 公布欄」可當天關掉。
- 版面：一個大主字＋一行說明＋最多三行內容；**上方保留今日課表細帶**（縮成一條，現在那節高亮，`0195a16`）。
- **放學畫面＝老師定稿的四句口訣**，黃色大字（`971c8ea`／`4f549ba`）。
- 老師可自訂公告：面板（鍵盤 `E`）→「公布欄公告」，一行一則。
- **上學前畫面改版（2026-09-08 老師回報）**：要交的作業原本只在**底部資訊帶、且只印第一項**，
  學生走進教室看不到 → 改成 **「今天要交」當大標、作業清單用黃色大字**（`mark:true`，與放學口訣同一級，
  最多 4 項），晨間 SOP 退到底部。沒有作業的日子才回到原本的「早安＋入班三件事」。
  **教訓：投影畫面的資訊層級＝學生的視線順序，底部資訊帶只適合放「順便看到就好」的東西。**

## Phase 3-4 作業清點改版（✅ 已完工上線 2026-09-08）

老師 2026-09-07 實跑一輪後提的三項，**當天全部上線並線上實測**。

1. **兩種版面可切換**（右上「🪑 座位表／🔢 座號清單」，選擇存 `classManager.homework.layout`，隔天維持）：
   - **座號清單**＝原版面，一列一份作業、座號 1→27 排開、完成即消失（**登記最快**）；附「顯示已完成」。
   - **座位表**＝一格一位學生，格內只留他**還沒完成**的作業徽章，交齊整格變暗（**看得出「誰」還沒交**）。
     預設「👥 全部（依學生）」是**一張座位表掛全部作業**（老師提案；一份作業一張表會疊四倍高度、
     1600×900 放不下）；點格子開側面板逐項改；上方膠囊可切成單一份作業的座位表（一點一格）。
   - 兩版面共用同一份狀態，互切不掉資料。**舊版面不因新版面上線而刪除**——登記快與認人快是兩種場合。
2. **每筆作業的鍵＝「派出日期｜名稱」**，store 升 `classManager.homework.v3`（**不從 v2 搬**：
   舊鍵沒有日期，硬搬正好把昨天的狀態掛到今天那列，就是要修的 bug）。
   起因：v2 用純名稱當鍵，「聯絡簿」天天同名 → 昨天點到完成的狀態今天照樣讀得到，
   老師被迫每天手動把整列還原成未交。**聯絡簿掛當天日期，隔天自動長出新的一列。**
3. **欠交看得出是哪一天**：每列掛 📅 派出日期；未交完的收進「⏳ 過去未完成（補交追蹤）」區
   （**無欠交時整區不出現**），照舊不進 `collect()`。結算送出的**備註也帶日期**
   （「國習 L2 P.10–11（09-07 派）」），週結才看得出是哪天的作業。
   追蹤逾 **14 天**只加紅字「已追蹤 N 天，該處理了」，**不自動下架**——靜默丟掉欠交紀錄比殘留更糟。

**踩雷（已修）**：flex 網格的 `min-width:auto` 會讓徽章多的格子自己撐大、走道空格又沿用舊寬度，
同一直行左緣差十幾 px，座位表對不上真實座位就失去意義 → `.agggrid`／`.hwgrid` 內**所有 `.scell`（含 `.gap`）
一律 `flex:0 0 固定寬`**。驗法＝各列首格 `getBoundingClientRect().left` 全等，**不要目測截圖**。

## 下一步（2026-09-08 收工盤點・第二次更新）

**第 2、3 項老師回報已完成，第 5 項結案不做——實際只剩兩件必做、一件選做。**

| # | 項目 | 狀態 | 備註 |
|---|---|---|---|
| 1 | **作業清點新版現場實跑一輪** | 必做 | 09-08 改版（兩版面切換／日期入鍵／過去未完成區）尚未經過真實上課。**v3 store 換代會讓「當天已點過的」歸零一次（僅此一次）**；開頁若見舊畫面按 Cmd+Shift+R |
| 2 | ~~用真實 tally 跑一次週結~~ | ✅ 老師回報已完成 | 建議下次週結**回讀帳本**確認事由是「第N週打掃薪水（X 次 × 2 幣）」且次數含支援／缺席／未達標——六個撈取鍵逐字比對，撈不到不報錯、只會算少 |
| 3 | ~~五站 → 收件匣 → R18 整批實測~~ | ✅ 老師回報 09-07 已實跑 | 要留驗收證據時再回讀當天紀錄庫列數與欄位 |
| 4 | **經驗庫修剪** | 必做 | `secondbrain/Claude_Code_經驗庫.md` 已破 50KB 紅線 → 跑 `Claude/RUNBOOK_規則修剪.md` 壓回 40KB |
| 5 | ~~公布欄公告改讀班網 announcements.json~~ | ❌ 結案不做 | 公布欄給**學生**看（這節做什麼、下節帶什麼），班網公告給**家長**看，性質不同不互接 |
| 6 | 公布欄其他時段的資訊層級複查 | 選做 | 09-08 修掉「上學前」後，同樣的毛病可能還在別的時段：`放學後／假日` 的作業走一般 `list`（中字）、`下課` 的「要帶什麼」也在 list。**投影畫面的資訊層級＝學生的視線順序**，等老師現場看過再決定要不要比照拉大 |

