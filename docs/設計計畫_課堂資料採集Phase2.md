# class-manager Phase 2 設計計畫：課堂資料採集與回寫

> **v4（2026-09-06）定案版**：在 v3 之上追加四項補充並全部拍板
> （打掃檢核逐人顆粒度＋三區監督人、廣播字幕、節慶倒數、座位板啟用範圍與每節重置）。
> v3 已拍板的設計爭點（管線、順序、班規檢核、報到板範圍、遲到幣值、抽問計幣、合併入庫）不變，
> **§6 的金幣通膨三題已解決，唯一動錢的路徑仍然只有班規**。
> 狀態：**設計定案・零待確認・尚未動工**（§4.3 對照表老師已於 09-06 看過確認）。
> 上位規則：`Project/AGENTS.md`、`班級事務/CLAUDE.md`、`class-manager/CLAUDE.md`。

---

## 0. 已拍板決策

| # | 議題 | 決定 | 拍板 |
|---|---|---|---|
| 1 | 回寫管線 | **A 案**：複用既有 `submit_task`，後端零改動，新增路由 R18，延遲 ≤30 分 | 09-05 |
| 2 | 工具順序 | 依建議；**追加「生活常規班規一併納入檢核」** | 09-05 |
| 3 | 出缺席 | 改成**學生到校自主報到**，超時未報到＝遲到 | 09-05 |
| 4 | 抽問門檻 | 一次就算一次 → **修正為：不論回答幾次，每生每科每天只算一筆** | 09-05 |
| 5 | 座位板顯示 | **顯示次數**（不顯示金額），讓師生當場核對紀錄是否正確 | 09-05 |
| **6** | **報到板範圍** | **只管「上學到校」**；**上課遲到走常規檢核台的課堂分頁** | 09-05 |
| **7** | **遲到幣值** | 上學、上課遲到**一律走紀錄庫「遲到 −5 幣」**（幣值相同，修復方式依來源自動帶不同的） | 09-05 |
| **8** | **抽問／舉手怎麼給錢** | **不論次數只算一筆**，事件描述固定寫 **「座號N 在○○課舉手回答」**；次數另存欄位 | 09-05 |
| **9** | **座位板計幣** | 座位板**單純計次**，入帳方式**比照 #8** | 09-05 |
| **10** | **紀錄庫筆數** | 收班時把「同學生 × 同天 × 同工具 × 同類」**合併成一列**，次數存欄位，計數完全保留 | 09-05 |
| **11** | **打掃檢核顆粒度** | **逐人檢核**（卡片顯示個人責任範圍）；資料走 Notion 補欄 →`duties-seats.json`，**不把紙本抄成本站靜態表** | 09-06 |
| **12** | **衛生監督人** | **三區三人各點各的**：內掃 **23**、外掃・圖書室 **16**、外掃・樓梯 **19**；監督生只產生候選，老師覆核才送出 | 09-06 |
| **13** | **電子看板小改版** | 語音廣播與鐘聲**一律配字幕**；節慶倒數接班網 `countdown.json`，**只顯示今天之後最近 1 筆** | 09-06 |
| **14** | **座位板啟用範圍** | **只在 `teacher==="導師"` 的節次啟用**；每節自動重置，該節結束才結成事件；入庫仍每天每科一筆 | 09-06 |

**#6–#10 共同的意義**：整套設計現在**只有一條動錢的路徑——班規**。
隨手加分不再直接變成錢，通膨風險解除（原 §6 試算：一週可達 +50 幣，商店券才 30–60 幣）。

---

## 1. 這份計畫要解決的事

老師的訴求：**「可量化的」日常與課堂行為，在教室當下點一點就進資料庫；
「需要描述情境的」輔導與個案，才用一句話指令交給 AI。**

現況這條線斷在中間：後台（Notion）與對話端（13 支 class-* skills）都完整，
但教室端依 `CLAUDE.md`「寫入分級」規定，**Phase 1 六支工具全部停在 L1：
只寫 localStorage、不新增任何寫入路徑**。老師在教室點的每一下現在全部丟掉。

---

## 2. 後台有什麼 × 教室端能採什麼

| 後台目的地 | 它需要的可量化資料 | 現在怎麼進去 | Phase 2 由誰採 |
|---|---|---|---|
| 📝 班經與學習紀錄庫（class-log / R01） | 座號・類別・科目・正負向・程度 | 老師事後口述 | 常規檢核台、座位加分板、三支既有工具 |
| 🏦 班級銀行（class-bank / R02・R03） | 金幣增減 | 週結從紀錄庫長出來 | **只走班規**（見 §0 #6–#10） |
| 🧹 打掃／午餐薪水 | **基準 ＋支援 −缺席** | 例外要老師記得 | 常規檢核台「環境晨掃」分頁 |
| 班級常規獎勵（每天+1、全勤+3） | 例外管理 | **沒人在記＝全班天天滿分** | 常規檢核台（各時段） |
| 📋 班規與獎懲（10 條卡） | 條號・行為・幣值・修復 | 老師口述或憑印象 | **常規檢核台（核心）** |
| 📊 學生學習報告 | 五向度＝該週紀錄庫彙整 | 紀錄庫夠密才準 | 以上全部 |
| 📒 聯絡簿／作業 | 繳交狀態 | 只在本機 | 作業清點回寫（工具已完成） |
| **上學遲到／一週全勤** | 到校時間 | **完全沒有** | **晨間報到板（§5）** |
| **上課遲到** | 哪一節、遲到幾分鐘 | 老師口述 | **常規檢核台・課堂分頁** |

---

## 3. 回寫管線（T0・定案）

### 3.1 走法

收班時把整批事件寫成**結構化任務原文**，POST 進既有 Apps Script 代理
（`class-website/scripts/apps-script-proxy-v2.gs` 的 `submit_task`，token 在 Script Properties、
前端只帶口令；代理網址在公開的 `data/site-config.json`，同源可讀）
→ 建「📥 任務收件匣」列 → 排程 Agent 依**新路由 R18** 批次入庫。

**後端零改動、不碰班網任何檔案、金幣公式只留一份。**

### 3.2 資料契約

```
#CM-EVENTS v1
{"tool":"routine","date":"2026-09-08","batch":"routine-20260908-01",
 "events":[
   {"id":"routine-20260908-01-s12-1","seat":12,"src":"rule",
    "rule_n":5,"kind":"bad","act_i":0,
    "act":"干擾課堂","coin":-5,"level":-1,"period":"上午課堂","subj":"數學","note":""},
   {"id":"checkin-20260908-01-s03-1","seat":3,"src":"rule",
    "rule_n":1,"kind":"bad","act_i":0,
    "act":"上學遲到","coin":-5,"level":-1,"at":"07:52"},
   {"id":"seatboard-20260908-01-s07-1","seat":7,"src":"tally",
    "kind":"good","act":"舉手回答","subj":"數學","count":3}
 ]}
```

- **新增路由 R18「批次事件包」**：命中條件＝任務原文以 `#CM-EVENTS` 開頭。
  執行層級 Sonnet、把關「直接完成（入庫）」——與 R01 同級，因為欄位已結構化、
  沒有語意判斷空間（模型無關性）。
- **座號→名冊 relation** 由排程 Agent 查 Notion 解析；**payload 只帶座號**（硬規則 2）。
- **`src:"rule"` 的 coin／level 是「原封轉抄 `class-rules.json`」不是前端計算**：
  R18 入庫前**回讀 `class-rules.json` 核對 `rule_n`＋`act_i` 的 coin／level 一致才寫入**，
  不一致就標失敗。既不讓前端算錢，又擋住班規改了而工具沒更新的漂移。
- **`src:"tally"` 一律不入帳**（金幣影響 0），只帶 `count`。見 §6。
- **防重複鍵＝ `id`**（工具×日期×批次×座號×序號）。按兩次收班、重送、隔天補送都不得重複入帳（U44）。
- **失敗即保留**：送出成功才清 localStorage；`ok:false` 或斷網 → 本機留著、標「尚未送出 N 筆」。

### 3.3 收班合併規則（拍板 #10）

送出前，把**同一學生 × 同一天 × 同一工具 × 同一類**的事件合併成一列：

| 來源 | 合併鍵 | 事件描述 | 次數 |
|---|---|---|---|
| 座位板／抽籤問答 | 座號 × 日期 × **科目** × 「舉手回答」 | **「座號N 在○○課舉手回答」**（拍板 #8 指定寫法） | 存「次數」欄 |
| 常規檢核（班規） | 座號 × 日期 × `rule_n` × `act_i` | 「⑤上課守秩序－干擾課堂」 | 同上 |
| 打掃檢核 | 座號 × 日期 × 三態 | 「晨掃未到」等 | — |

- **座位板每節重置**：該節結束才把有次數的學生各結成一筆待送事件；
  同科目一天有兩節時，收班時加總成同一列（詳見 §5A.3）。
- **紀錄庫需新增一個 `次數`（number，選填）欄**——小改動，class-log／class-report 現有欄位不動。
  （替代方案：次數寫進事件描述末尾括號「（3 次）」，零 schema 改動；建議用欄位，報告端好統計。）
- **原始逐筆時間戳仍留在本機 localStorage**，需要細查時看得到。

### 3.4 憑證擺放（投影場景專屬風險）

教室投影全班看得到畫面，所以定死：

- **只有工作台 `index.html` 能送出**；收班在工作台按，不在投影工具頁按。
- 工具頁只累積 localStorage，**不 import 任何送出程式碼、不持有口令**。
- 口令輸入用 `type=password`，送出後不回顯，**永不寫 localStorage**（只留 sessionStorage）。

---

## 4. 常規檢核台 `routine.html`（核心工具）

盤點後發現：**班網公開的 `data/class-rules.json` 已經是一份完整的結構化正本**，
不必另外建資料，直接拿來當工具的骨架。

### 4.1 它已經有的三層資料

| 區塊 | 內容 | 在工具裡當什麼 |
|---|---|---|
| `cards`（10 張） | 班規條號・公約對應・`good[]`／`bad[]`（具體行為・幣值・程度・**修復方式 fix**） | **事件目錄**：點一下就帶齊五個欄位 |
| `daily`（8 段） | 早晨入班／環境晨掃／朝會晨讀／上午課堂／午餐／潔牙午休／下午課堂／整理放學，各有 `sop`／`expect`／`redo` | **檢核的時機＝分頁** |
| `flows`（6 個） | 進教室／發材料／上課發言／小組討論／收拾／排隊，各有 `redo` | 當場「重來一次」的指示卡 |

### 4.2 架構：一支工具、八個時段分頁

**不做三支獨立工具**（原 v1 的打掃檢核＋常規檢核＋班規記錄），改成一支 `routine.html`，
用 `daily` 的 8 個時段當分頁。每個時段頁面分上下兩半：

- **上半（投影給學生看）**：該時段的 `expect`「做到的樣子」。
  常規不再是貼在牆上的紙，而是每個時段都出現在眼前。
- **下半（老師操作）**：座號網格。點一位學生 → 彈出**該時段相關的班規行為選單**
  （從 `cards` 依 §4.3 對照表過濾，一頁通常 4–8 個選項）→ 再點一下完成。

### 4.3 時段 → 班規對照表（**動工前需老師確認，這是教學專業判斷**）

| 時段 | 主要相關班規卡 |
|---|---|
| 早晨入班 | ① 不遲到（上學）、⑦ 管好自己 |
| 環境晨掃 | ③ 打掃要認真 |
| 朝會／晨讀 | ⑤ 上課守秩序、⑦ 管好自己 |
| 上午課堂／下午課堂 | **① 不遲到（上課）**、⑤ 守秩序、⑨ 上課時間、④ 作業用心、② 有禮貌、⑧ 口說好話 |
| 午餐 | ⑦ 管好自己、③ 打掃（餐後整理） |
| 潔牙午休 | ⑦ 管好自己 |
| 整理放學 | ③ 打掃、⑥ 不奔跑、⑦ 管好自己 |
| **任何時段都可叫出** | ② 有禮貌、⑧ 口說好話、⑥ 不奔跑、⑩ 重大安全事件 |

### 4.4 上課遲到（拍板 #6・#7）

**上課遲到不進報到板，走課堂分頁的班規 ①。** 班規卡 ① 的兩個 bad 項幣值相同、修復不同，
工具依來源自動帶對的那一個：

| 來源 | 行為 | 幣值 | 修復方式（`fix`，點完當場投影） |
|---|---|---|---|
| 晨間報到板 | 上學遲到 | **−5** | 補做打掃工作及課堂指定作業 |
| 常規檢核台・課堂分頁 | 上課遲到 | **−5** | 回教室先向老師報到，下課把落掉的進度補齊，**下課靜坐到遲到的時間**（遲到 5 分鐘就靜坐 5 分鐘） |

→ 上課遲到要記「遲到幾分鐘」時，寫進事件描述末尾（靜坐時間由此而來）。

### 4.5 三條設計鐵則

1. **一律例外管理，預設全班達成，老師只點例外。**
   多數時段是 0 動作。若要老師每個時段點 27 個人，這支工具一週就會被放棄。
2. **點完當場顯示「修復方式」給學生看。**
   例：`座號12 · ⑤上課守秩序 · 修復：到冷靜區沉澱 5 分鐘`。
   **這是這支工具最大的教育價值**——懲處不是只扣分，當場就講清楚怎麼修復；
   而且 `fix` 文字是老師自己在 Notion 寫的，AI 沒有發揮空間。
3. **不在投影上排名、不做全班排行榜**（通用鐵則 6；同「禁用 A／B／C 組」的去標籤化精神）。

### 4.6「環境晨掃」分頁＝個人打掃檢核表（拍板 #11・#12）

紙本正本＝`115班級內外掃區個人打掃檢核表-v3.pdf`（一週一張、逐人一列）。這支分頁就是它的投影版。

**顆粒度＝逐人，不是逐組。** 紙本每個人都有自己的責任範圍
（窗戶組 2號北側／3號南側／14號前側／9號後側；室內排組 4號第1-2排／22號第3-4排／8號第5-6排…），
檢核台必須點得到「人」，也顯示得出「你負責哪一塊」。

- **畫面**：一頁一個掃區，列出該區成員座號卡，**卡片副標＝該生的個人責任範圍**，
  三態：`✓ 到位達標`／`△ 到位未達標`／`✗ 未到`（對應紙本的 ✓／Δ／X）。預設全 ✓。
  點卡片可展開該區的 `title`／`work`／`authority`／`standard`
  （＝紙本的【工作職稱】【具體職責 SOP】【授權範圍】【驗收標準】，`duties.json` 已有這四欄）。
- **三位衛生監督人分三區各點各的**（拍板 #12）：

  | 區 | 監督座號 | 檢核範圍 |
  |---|---|---|
  | 內掃區 | **23** | 窗戶組・室內排組・走廊組・垃圾組・黑板櫃桌組 |
  | 外掃區・圖書室 | **16** | 圖書室內（掃地／擦地櫃）・圖書室外（走廊掃地／拖地／洗手台／鞋櫃女兒牆） |
  | 外掃區・樓梯 | **19** | 地下室–1F・1F–2F・2F–3F・3F–4F 各段 |

  進頁面先選「我是哪一區的監督」（老師視角可看全部）。
  **監督生只評自己那區，老師覆核後才送出**——機器與學生都只產生候選，老師確認才成立（同 §5.3 原則）。
  16 與 19 本身也有打掃工作，**自己那一格由老師評，不自評**。
- **本週總覽**：另一個檢視是「座號 × 週一～週五」網格，直接對上紙本那張表，**投影即可取代列印**。
- **回寫**：
  - `✓` → **不產生事件**（走週結的固定基準，零筆最省）
  - `△` → 班規 ③ bad「負責的區域沒掃乾淨」−5 幣、修復「下課回去重掃到通過檢查」
  - `✗` → **打掃缺席事件**，週結薪水次數 −1（正是 class-bank 現行公式的 `−缺席次數`）
  - 臨時支援 → `+支援次數`
- **資料來源（工單擴充，見 §7 的 2-2）**：`duties.json` 只到「組」層級，且只有去識別化姓名，
  **紙本的座號與個人責任範圍在 Notion 裡還沒有欄位**。做法＝
  **在 Notion「🧹 班級工作分配」補「座號」與「個人責任範圍」兩欄** → sync 輸出
  `duties-seats.json`（純座號、含個人責任範圍與三區監督人）。
  ⚠️ **禁止把 PDF 內容抄成 class-manager 的靜態表**——換座位／換掃區時它不會跟著改，
  就變成一份會說謊的第二正本。
  **拿不到時退回一般座號網格**，工具照樣可用（只是少了掃區分組與責任範圍）。
- **價值**：現在這些例外全靠老師記得，漏記＝薪水算錯錢。機械化後**週結金額才真的可信**。

### 4.7 類別對照（寫進 SPEC，不留給實作自由心證）

| 班規卡 | class-log 類別 |
|---|---|
| ③ 打掃認真 | 生活技能 |
| ④ 作業用心 | 作業 |
| ⑤ 上課守秩序、⑨ 上課時間 | 課堂表現 |
| ② 有禮貌、⑧ 口說好話 | 人際互動 |
| ① 不遲到、⑥ 不奔跑、⑦ 管好自己 | 生活指導 |
| ⑩ 重大安全事件 | 生活指導・**金幣 −200 定額、不算程度**（比照 class-log 定額項目規則） |

---

## 5. 晨間報到板 `checkin.html`

### 5.1 範圍（拍板 #6）

**只管「上學到校」這一件事。上課遲到不在這裡，走 §4.4 的常規檢核台課堂分頁。**

三個功能，第三個價值最大：

1. 每天 30 秒完成到班確認；
2. 產生**上學遲到候選名單**給老師確認；
3. **自動累積班規 ① good「一整週都沒有遲到 +10 幣」**——目前完全沒在發，因為沒資料可算。

### 5.2 做法：學生自己點，老師只處理例外

門口一台平板（或教室電腦）開 `checkin.html`：座號網格，學生到校**按住自己的格子 1 秒**完成報到。
純 localStorage、離線可用、只有座號與時間戳，符合硬規則 2 與 4。

**老師端一律例外管理**：截止時間到 → 自動鎖定 → 畫面只剩「候選遲到」那幾格要處理，
**沒有例外就 0 動作**（與常規檢核台同一個操作邏輯）。

**動線設計比程式更重要**：依 `class-rules.json` 的 `daily` 早晨入班 SOP
「掛書包 ➜ 抄聯絡簿 ➜ 交作業 ➜ 回座」，**報到插在「交作業」那一步**，
孩子本來就要走到那裡，不額外增加動線，也不會塞車。

### 5.3 五個必須設計進去的風險

| 風險 | 對策 |
|---|---|
| **① 代點／誤點**（小四最大風險） | 按住 1 秒才算（防手滑）＋點完變色並播一聲（旁人會注意）＋**點過自己不能取消**（要改得老師解鎖）＋**時間戳全留可查** |
| **② 沒點到 ≠ 遲到** | 請假、公假、去健康中心、被科任叫走都可能沒點。截止後畫面分三欄**未報到（候選遲到）／請假／已報到**，老師把非遲到的移走，**只有留在候選欄的才送出** |
| **③ 裝置塞車** | 格子要大（平板橫放一格 ≥100px）、一秒完成；**Phase 1 先單機**，實測真的塞車再評估雙機（雙機要處理合併，不要一開始就做） |
| **④ 老師忘了開／裝置沒電** | **沒開＝沒有資料，不是全班遲到**。程式必須有「今天未啟用報到」狀態，收班時**不產生任何事件**。這條要寫死進 SPEC，否則某天忘了開就送出 27 筆遲到（U41：沒有資料 ≠ 這個人不存在） |
| **⑤ 公開標籤化** | 截止前只顯示「已報到＝亮／未報到＝暗灰」，**不標紅、不寫「遲到」二字**；截止後自動切老師視角或關閉投影，候選名單只在老師裝置上看 |

**最關鍵的是 ②＋④ 背後的同一個原則：機器只產生候選，老師確認才成立。**
符合通用鐵則 1（排程後 Check 仍須人為把關），也把代點與漏開的傷害上限壓住。

### 5.4 回寫（拍板 #7）

- 上學遲到 → 紀錄庫「生活指導」・負向・程度 1・**金幣 −5**，
  事件描述「座號N 上學遲到（07:52 報到）」，修復＝班規①「補做打掃工作及課堂指定作業」。
- 一週全勤 → 紀錄庫「生活指導」・正向・程度 2・金幣 +10（週結時由報到資料機械算出）。

### 5.5 **Notion 不必開新表**

遲到已經是班規 ① 的既有懲處，直接走紀錄庫即可，**省一整個工作段**。
什麼時候才需要開「🗓️ 出缺席紀錄」表？當你需要「本學期遲到幾次」的統計或行政報表時，
到那天再開，屆時可從紀錄庫回填。事假／病假等真正的缺席仍走**老師一句話補登**
（那是低頻、需要情境描述的事，不塞進這支工具）。

---

## 5A. 座位加分板 `seatboard.html`（拍板 #9・#14）

### 5A.1 版型＝班網座位表

`class-website/data/seating.json` 已是 6 欄 × 5 列的 grid
（`columns` 由左至右＝第六排→第一排，列由上至下＝前排→後排，講台在最上方），
但**只有去識別化姓名、沒有座號**，而本站一字不存姓名 →
需 §7 的 2-2 工單輸出 `seating-seats.json`（純座號 grid，欄列結構相同）。
拿不到就退回一般座號網格（漸進增強，離線照樣可用）。

### 5A.2 只在導師的課啟用（拍板 #14）

`schedule.json` 的 `table` 每一格都有 `teacher` 欄，**導師課寫的就是 `"導師"`**（科任課寫科任老師姓名）。
**只有 `teacher === "導師"` 的節次才開座位板**；科任節次顯示
「這節是○○老師的課，座位板不啟用」。理由：科任課老師不在教室用本系統，
照樣計次會產生沒有人負責、也沒有人核對的資料。

### 5A.3 每節重置、該節結束才結成事件（拍板 #14）

依 `schedule.json` 的 `periods` 時間自動判斷現在第幾節，**換節自動歸零**並提示
「第三節 數學・計次已重新開始」；老師也可手動按「新的一節」。

- **當節畫面**：格子上顯示**當節次數**（不顯示金額），師生當場核對紀錄對不對（拍板 #5）。
  次數的意義是「這節有誰發表過」，不是分數。
- **該節結束**：有次數的學生各結成一筆「舉手回答」待送事件存 localStorage
  （`src:"tally"`，帶 `subj` 與 `count`）；**沒次數的不產生任何事件**。
- **收班入庫**：同 §3.3——「座號 × 日期 × **科目**」合併成一列，
  事件描述固定寫「座號N 在○○課舉手回答」，次數加總存「次數」欄。
  **同一天同科目兩節會併成一筆，這是刻意的**：不動錢，細分到節次只增加 Notion 筆數。
  要查是哪一節，看 localStorage 的逐筆時間戳。
- **不入帳**：`src:"tally"` 金幣一律 0（§6 定案）。要動錢請長按開班規選單走 `src:"rule"`。

---

## 5B. 電子看板小改版：廣播字幕＋節慶倒數（拍板 #13）✅ 已完工 2026-09-06

> **完工回填**：commit `0400b76`＋字幕下限修正，線上實測通過（1600×900 未溢出、390 寬無橫向捲動、
> 節慶倒數讀到「中秋節連假 還有 19 天」、SOP 摘要在早晨／上課／午餐三個時段都取到正確文案）。
> 實作時多修一道：裝置沒有中文語音時 `onerror` 會立刻回，字幕原本只閃 0.6 秒，
> 已改成「語音講完才收，但至少撐滿字數÷4.5、下限 3 秒」——正是本節要解決的情境。

**這一項不碰回寫管線、零風險，排在 2-0 之前先出**（§7 的 2-A），老師可以立刻用到。

### 5B.1 語音廣播與鐘聲一律配字幕

`blackboard.html` 已有 `.toast` 大字提示（上下課鐘聲在用），但 `speak()` 目前**只出聲不出字**。
把兩者接起來，**學生看得到現在該做什麼**，順便解決「這台裝置沒有中文語音」的情況
（字幕從備援變成主要通道）。

- **顯示時長跟著句子長度**：用 `SpeechSynthesisUtterance.onend` 收字幕；
  沒有語音引擎時退回「字數 ÷ 4.5 字/秒」估算，**下限 3 秒**。
- **長句要能換行**：toast 目前是單行大字，需加 `max-width:80vw` 與 `white-space:normal`，
  否則 1600×900 會溢出（完工要就地量測，見 §8）。
- **鐘聲字幕帶上「現在該做什麼」**：現行只寫「第三節 上課」，改成
  「🔔 第三節 數學 上課了　課本、習作、鉛筆盒準備好」。
  科目由 `schedule.json` 帶出；**準備動作的文案來源＝`class-rules.json` 的 `daily.sop`，
  不寫死在程式裡**（文案是老師在 Notion 寫的，AI 沒有發揮空間）。

### 5B.2 節慶倒數接班網 `countdown.json`

`class-website/data/countdown.json` 已存在（`title`／`date`／`emoji` 三欄，正本在 Notion），
同源 fetch、免 token、漸進增強，**不必另外寫一份資料**。

- **只顯示今天之後最近的 1 筆**（拍板 #13）：顯示多筆會讓學生一直想著放假。
- 格式「🥮 中秋節連假 還有 19 天」，位置在月相時鐘旁。
- **命名要分開**：看板右下角現有的「倒數」是分鐘計時器 → 改稱「**計時**」，
  新的叫「**節慶倒數**」，避免師生混淆。

---

## 6. 金幣通膨：三題已解決（拍板 #8–#10）

原問題：class-log 公式 `金幣 = 正負向 × 程度 × 5`，課堂表現程度 1 ＝ **+5 幣**。
若抽問「每次都入帳」，單一學生一週答對 10 次 ＝ **+50 幣**，
對照打掃薪水一週 10 幣、商店券 30–60 幣 → 班級經濟會崩。

### 定案

| 項目 | 定案 |
|---|---|
| **抽問／舉手回答** | **不論回答幾次，每生每科每天只算一筆**；事件描述固定寫「**座號N 在○○課舉手回答**」，次數存「次數」欄 |
| **座位加分板** | **單純計次**（格子上顯示次數供師生核對），入帳比照上一列 |
| **合併入庫** | 收班時「同學生 × 同天 × 同工具 × 同類」合併成一列，計數完全保留 |

### 效果

- **唯一動錢的路徑是班規**（`src:"rule"`）。座位板輕點與抽問都是 `src:"tally"`，**不入帳**、
  只累積次數供學習報告的「課堂表現」向度使用。
- **實務量級**：一位學生一天最多 3 科各一筆 → 逐筆 40 筆/天 壓成 ≤ 8 筆/天，
  一學期由約 2,000 筆降到數百筆，Notion 查詢配額壓力解除。
- **拍板 #5 的透明度需求照樣滿足**：投影上看得到次數，學生當場能核對自己被記了幾次。

---

## 7. 路線圖

| 階段 | 內容 | 前置 | 預估 |
|---|---|---|---|
| **2-A** | ✅ **已完工（2026-09-06 上線）**：廣播／鐘聲字幕＋節慶倒數＋「倒數」改稱「計時」（§5B） | 無 | 已完成 |
| **2-0** | T0 管線＋R18 路由＋工作台「今日待送」面板＋紀錄庫加「次數」欄 | §4.3 對照表確認 | 1 個工作段 |
| **2-1** | **只接作業清點一支**，實際用滿一週跑 PDCA | 2-0 | 半個工作段 |
| **2-2** | **Notion＋班網工單**：①Notion「🧹 班級工作分配」補「座號」「個人責任範圍」兩欄 ②sync 多輸出 `duties-seats.json`（含個人責任與三區監督人）／`seating-seats.json`（純座號 grid）。**這是 Notion 與 class-website 的改動，另開** | 可與 2-0 併行 | 半個工作段 |
| **2-3** | 常規檢核台 `routine.html`：先做**環境晨掃＋上午課堂**兩個分頁 | 2-0＋2-2 | 1 個工作段 |
| **2-4** | 座位加分板 `seatboard.html`（§5A：座位表版型・只在導師課啟用・每節重置・輕點計次／長按班規） | 2-0＋2-2 | 1 個工作段 |
| **2-5** | 晨間報到板 `checkin.html`（只管上學；不建新表，走紀錄庫） | 2-0 | 1 個工作段 |
| **2-6** | 常規檢核台補齊其餘 6 個時段分頁 | 2-3 實用一輪後 | 半個工作段 |
| 後續 | 抽籤問答／小組計分回寫、行為計次器、隨堂小考登分板、課堂拍照上傳 | — | — |

**PDCA 節點**：2-1 只接一支、**實際用滿一週**，確認 ①收班不重複入帳 ②紀錄庫沒被灌成噪音
③週結金額正確——三項都過，才做 2-3 之後的（通用鐵則 1）。

---

## 8. 踩雷檢查表（動工時逐條核）

- **收班要冪等**：`id` 三段式，按兩次不重複發錢（U44）。
- **零事件的學生 ≠ 不存在**：報表端先開空殼再累加（U41）。
- **報到板沒開＝沒有資料**，不得產生任何遲到事件（§5.3 ④）。
- **公開輸出的過濾做在產生端**：遲到／班規負向紀錄**絕不進班網公開 JSON**（U42）。
- **金幣公式只留一份**：前端不算錢；`src:"rule"` 的幣值由 R18 回讀 `class-rules.json` 核對；
  `src:"tally"` 一律金幣 0。
- **R18 會觸發第五同步**：必須同步改 `07_排程Agent_routine_prompt.md`，並用 `RemoteTrigger`
  更新雲端兩條 routine 的 prompt，改完回讀確認 `enabled`／`cron_expression`／`mcp_connections` 未被動到。
- **班級鐵則 8**：每支工具上線同步 `docs/老師操作手冊.md` ＋ Notion 使用說明（歸既有分類）。
- **完工定義**：push 後**輪詢**到 200／指紋出現（單發 curl 會誤判，U46）；1600×900 無溢出
  （就地 `height:auto` 讀 `scrollHeight`）；390 寬無橫捲；靜態資源版本號往前推。

---

## 9. 動工前確認：已全部完成（2026-09-06）

- ✅ **§4.3 時段→班規對照表**：老師已看過確認，照表實作。
- ✅ **紀錄庫「次數」欄**：定案為**新增 `次數`（number，選填）欄**，
  不採「寫進事件描述括號」的替代做法——報告端要能統計。

**本計畫零待確認，可直接進 §7 路線圖的 2-A 開工。**
